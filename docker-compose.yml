services:
  rikkahub:
    # build:
    #   context: .
    #   dockerfile: Dockerfile
    # Image tags:
    # - amd64: ghcr.io/longyq20/rikkahub:webdist
    # - arm64: ghcr.io/longyq20/rikkahub:webdist-arm64
    image: ghcr.io/longyq20/rikkahub:${RIKKAHUB_IMAGE_TAG:-webdist}
    container_name: rikkahub
    ports:
      - "8080:8080"
    environment:
      HOST: 0.0.0.0
      PORT: 8080
      DATA_DIR: /data
      WEB_UI_DIR: /app/web-ui
      ASSETS_DIR: /app/assets
      JWT_ENABLED: "false"
    volumes:
      - ./data:/data
    restart: unless-stopped
